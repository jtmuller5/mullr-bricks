import 'package:flutter/material.dart';

class Large{{#titleCase}}{{name}}{{/titleCase}} extends StatelessWidget {
  const Large{{#titleCase}}{{name}}{{/titleCase}}({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
