import 'package:flutter/material.dart';

class Small{{#titleCase}}{{name}}{{/titleCase}} extends StatelessWidget {
const Small{{#titleCase}}{{name}}{{/titleCase}}({Key? key}) : super(key: key);

@override
Widget build(BuildContext context) {
  return Container();
}
}